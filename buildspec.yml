version: 0.2

phases:
  install:
    commands:
      - echo Installing dependencies...
      - cd backend
      - npm install

  build:
    commands:
      - echo Build started...
      - npm test || echo "No tests yet"

artifacts:
  files:
    - '**/*'